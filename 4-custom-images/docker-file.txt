################################################################################
# Dockerfile
#
# docker build -f docker-file.txt -t andrex873/ubugit:0.2 .
################################################################################

# Base Image
FROM ubuntu:16.04

# Install the packages
RUN apt-get update && apt-get install -y git
